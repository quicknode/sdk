// Exported main sdk file that imports from autogenerated napi-rs file

const { init: _init, HttpbinClient: _HttpbinClient } = require("./index.js");

let httpbin = null;

class HttpbinClient {
  constructor() {
    this.client = new _HttpbinClient();
  }

  async getUuid() {
    return this.client.getUuid();
  }
}

function init(apiKey) {
  _init(apiKey);
  httpbin = new HttpbinClient();
}

module.exports = {
  init,
  get httpbin() {
    return httpbin;
  },
};
